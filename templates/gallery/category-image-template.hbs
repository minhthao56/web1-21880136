<div class="row">
        {{#each data}}
          <div class="col col-lg-3">
            <img
              src="{{thumbpath}}"
              alt="{{name}}"
              class="img-thumbnail mb-3"
            />
          </div>
        {{/each}}
      </div>